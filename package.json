{
  "name": "grid_colors",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --open --host",
    "build": "vite build",
    "preview": "vite preview --open --host",
    "deploy": "surge ./dist moej-kleurtjen-en.surge.sh"
  },
  "devDependencies": {
    "vite": "^6.0.5"
  }
}
